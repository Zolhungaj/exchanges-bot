export const uncheckSymbol = '☐';
export const checkSymbol = '☒';

export const groupToken = '%group%';
export const groupFormat = `**Group ${groupToken}**`;
export const scoreToken = '%score%';
export const scoreFormat = `**(${scoreToken})**`;

export const groupPrefix = 'Group:';
export const entryPrefix = 'Anime Given:';
export const scorePrefix = 'Score:';

module.exports = {
	uncheckSymbol, checkSymbol,
	groupToken, scoreToken,
	groupFormat, scoreFormat,
	groupPrefix, entryPrefix, scorePrefix,
}